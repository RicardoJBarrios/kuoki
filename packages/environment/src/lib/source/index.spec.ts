describe('EnvironmentSource Use Cases', () => {
  it(`Fallback sources`, () => {
    expect(true).toBeTrue();
  });

  it(`Use values from other sources`, () => {
    expect(true).toBeTrue();
  });
});
