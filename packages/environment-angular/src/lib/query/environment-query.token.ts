import { InjectionToken } from '@angular/core';
import { EnvironmentQuery } from '@kuoki/environment';

export const ENVIRONMENT_QUERY: InjectionToken<EnvironmentQuery> = new InjectionToken('ENVIRONMENT_QUERY');
